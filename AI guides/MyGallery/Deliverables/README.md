# Deliverables

A release, and only once the user has confirmed it final: `<stem> v<version>.docx`, built from the released Markdown. `build_docx.py` refuses to write a draft here.

Every document in the set releases into this one folder, so a handover is this folder.
