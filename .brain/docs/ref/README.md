# ref/

Screenshots, emails, and other files dropped next to a `/note`.

Named `NNN-original.ext` (serial, then the file name). The dashboard Working
tab lists serial, filename, type, and an icon.

Do not put secrets here.
