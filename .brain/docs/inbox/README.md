# inbox/

Drop a PDF, screenshot, or any other file here, then run `/dashboard` or `/note`.
The file is moved to `ref/` as `NNN-name.ext` and listed on the Others tab.

Chat attachments are copied into `ref/` when you send the message, if Cursor
gives the hook a path. If it does not, put the file here.

Do not put secrets here.
