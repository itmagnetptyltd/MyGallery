# Source brief

Received: 2026-09-18, from the client (project owner), via message

> Create a simple MyGallery app. I want to upload photos, see them in a
> gallery, click one to see it larger, and be able to delete and download a
> photo. Keep it easy to run on my PC.

Nothing else was said.

---

## Ground truth at the time of decomposition

There is no running code. `src/` contains only `.gitkeep`. This is a
greenfield brief: nothing on disk settles any of the questions below, so
nothing may be closed by reading the repository.

The module code is `GAL`, taken from `.brain/install-state.json`.
