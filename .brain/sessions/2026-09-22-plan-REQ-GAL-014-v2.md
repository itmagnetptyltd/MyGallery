# Plan — REQ-GAL-014@v2

Slice 7, UAT visual. Planned together with REQ-GAL-012@v3 in
`2026-09-22-plan-REQ-GAL-012-v3.md`, which holds the full plan for both.

REQ-GAL-014@v2 needs no new code. c1 (the close control inside the Larger
view panel) is unchanged; c2 (the panel's actions are Save description and
Close, with no Download or Delete) is the same text as REQ-GAL-004@v3 c7,
built and tested in slice 3.

## Tests

- Move `@covers REQ-GAL-014@v1` → `@v2` on the two close-control tests for c1,
  in `tests/test_uat_visual.py` and `tests/e2e/test_uat_visual_browser.py`.
- Add `@covers REQ-GAL-014@v2` for c2 to
  `tests/test_larger_view_actions.py::test_the_larger_view_actions_are_save_description_and_close` (A)
  and
  `tests/e2e/test_larger_view_actions_browser.py::test_the_larger_view_shows_save_description_and_close_side_by_side` (B).
