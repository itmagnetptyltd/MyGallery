# Plan — REQ-GAL-012@v3 and REQ-GAL-014@v2

Slice 7, UAT visual — the two ids left before it is Done again. REQ-GAL-013@v3
is `verified`. Both versions come from FB-0006: REQ-GAL-012@v3 from CHG-0015
and CHG-0016, REQ-GAL-014@v2 from CHG-0014.

Gate: `close-slice.js --gate REQ-GAL-012` passed — slices 1-6 are Done.
Dependencies REQ-GAL-003 (v4) and REQ-GAL-004 (v3) are `verified`.

Neither requirement needs new code. Each repeats criteria that a slice
before it has already built and tested, as its `source` says ("The same
criteria are on REQ-GAL-003@v4" / "on REQ-GAL-004@v3"). This slice is
traceability: the tests that prove those criteria also cite these ids.

## What must be true

REQ-GAL-012@v3:

1. **c1** Each card is wider than the 160-pixel v1 tile. Unchanged.
2. **c2** Each Thumbnail sits in a card with a visible surface. Unchanged.
3. **c3** A Thumbnail's alt text is its Photo's description. Unchanged.
4. **c4** With the pointer on no card, each card shows Download and Delete
   below its image. The same text as REQ-GAL-003@v4 c11, built in slice 2.
5. **c5** A card's Download and Delete are the same width. The same text as
   REQ-GAL-003@v4 c12, built in slice 2.

REQ-GAL-014@v2:

1. **c1** The close control is inside the Larger view panel. Unchanged.
2. **c2** The panel's actions are Save description and Close, side by side,
   with no Download or Delete. The same text as REQ-GAL-004@v3 c7, built in
   slice 3.

## Approach

Tests only. No file under `src/` changes.

## Tests

Pin moves — re-read, then move:

- `@covers REQ-GAL-012@v2` → `@v3` on the six tests for c1-c3:
  `tests/test_uat_visual.py` (3) and `tests/e2e/test_uat_visual_browser.py` (3).
- `@covers REQ-GAL-014@v1` → `@v2` on the two close-control tests for c1:
  `tests/test_uat_visual.py` and `tests/e2e/test_uat_visual_browser.py`.

Added annotations — one more `@covers` line on tests that already prove the
repeated criterion. Each test keeps its existing pin; the project rule is one
requirement per annotation, several annotations per test.

| Test | Adds | Criterion |
|---|---|---|
| `tests/test_card_actions.py::test_a_thumbnail_card_is_built_with_download_and_delete_controls` | `REQ-GAL-012@v3` | c4 (A) |
| `tests/test_card_actions.py::test_the_card_controls_share_the_row_in_equal_columns` | `REQ-GAL-012@v3` | c5 (A) |
| `tests/e2e/test_card_actions_browser.py::test_every_card_shows_download_and_delete_without_hovering` | `REQ-GAL-012@v3` | c4 (B) |
| `tests/e2e/test_card_actions_browser.py::test_a_cards_download_and_delete_are_the_same_width` | `REQ-GAL-012@v3` | c5 (B) |
| `tests/test_larger_view_actions.py::test_the_larger_view_actions_are_save_description_and_close` | `REQ-GAL-014@v2` | c2 (A) |
| `tests/e2e/test_larger_view_actions_browser.py::test_the_larger_view_shows_save_description_and_close_side_by_side` | `REQ-GAL-014@v2` | c2 (B) |

Both requirements need belts A and B; the table gives each criterion one of
each.

## Decisions this forces

None.

## What I am unsure about

- **Whether re-citing a test counts as covering.** It is the same assertion
  against the same text, so a second test would only duplicate it. If the
  project would rather each requirement own its own tests, say so and I will
  write copies under `tests/test_uat_visual.py` instead.
